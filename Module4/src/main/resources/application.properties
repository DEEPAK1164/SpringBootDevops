spring.application.name=Module4

spring.datasource.url=jdbc:mysql://localhost:3306/Module4DB?useSSL=false
spring.datasource.username=root
spring.datasource.password=#pyG4XsLkN

spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
server.port=8083
employeeService.base-url=http://localhost:8080

springdoc.swagger-ui.url=/v3/api-docs
springdoc.swagger-ui.disable-swagger-default-url=true

#logging configuration
logging.level.com.example.Module4.client.Impl=TRACE
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n

logging.file.name=application.log
#logging.config=classpath:logback-spring.xml

#actuator configuration
management.endpoints.web.exposure.include=*
management.info.env.enabled=true
info.abcd=pqrs
info.app.author=Deepak Maurya
info.app.version=1.0.0

##it seeds the db with some dummy data
#spring.jpa.defer-datasource-initialization=true 
#
##`spring.sql.init.mode=always` forces Spring Boot to run `schema.sql` and `data.sql` on **every application startup**, for **all database types**.
#spring.sql.init.mode=always
#
##seeded data comes from this locaion
#spring.sql.init.data-locations=classpath:data.sql